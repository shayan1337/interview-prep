package linkedhashmap;


/**
 * Represents a linked hashmap data structure where every node is connected to a next node and also hashed my a map
 * APIs include:
 * 1) insert(val) -> inserts an element with a particular value if not already present
 * 2) remove(val) -> removes an element with a particular value if present
 * 3) getRandom -> returns a random element with equal probability
 */
public class LinkedHashMap {
}
